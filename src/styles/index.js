//transition: box-shadow 0.4s ease-out;

export const generalStyles = (theme) => ({
    flex: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center'
    }
  })